services:
  - type: web
    name: bushra-tech-playground-backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: CHAT_KEY
        sync: false
      - key: PORT
        value: 3000

